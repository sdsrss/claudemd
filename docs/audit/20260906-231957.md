# 全局规范提示词专项审核 — Round 15（2026-09-06 23:19 UTC）

审核对象：`spec/CLAUDE.md` v6.26.0（core，23 944 B）+ `spec/CLAUDE-extended.md`（45 899 B）+ `spec/OPERATOR.md` + `spec/hard-rules.json`，仓库 `main` @ `a7d9f1a`（tag `v0.78.0`）。
本机实际生效版本：`~/.claude/CLAUDE.md` = **v6.25.4**（插件 0.76.1），落后仓库一个 minor。
审核方式：只读，主审单人。工具输出全部本轮新鲜产生；未改任何 spec / hook / 代码。副作用一个：`scripts/sampling-audit.js` 按设计写了 `tasks/sampling-audit-2026-09-06.md`（gitignore 内，§13.4 表标 manual prune）。
上一轮（Round 14，`docs/audit/20260906-230810.md`）已覆盖规范的 4 High / 22 Medium；本轮不重复其逐条核实，只在 §8 标注状态，重点回答用户提出的四个问题。

---

## 0. 一句话总评

**规范不是"版本老"，而是"疤痕厚"：文本本身两天新，但 core 处于 25 K 硬上限的 95.8%，其中约 3–4 KB 是为早期模型误读写的澄清、为单次事故写的实测理由、以及与 2026-09 harness 系统提示重复甚至冲突的段落。** spec 化（§2.S）、信号标准化（只有两个信号）、hook 自动化（15 个 hook，单次开销 ≤35 ms）三项都真实落地并有使用证据；hook 侧最显眼的摩擦是 §8 rm-rf 门 1.3 天 93 次 deny（28/36 会话至少一次），但逐条核对 `extra.matched` 后，这些全是 `rm -rf $S` 类未校验变量，即规则按设计生效；真正的误拦类问题是 pattern 3d（HK-H1），频次低但形状明确（**2026-09-07 更正**：初稿把 93 次 deny 归因为 HK-H1，有误）。建议下一版走 **"harness 收敛 + 净删除"** 路线，目标 core −3 KB、extended −2 KB，零新增 HARD。

---

## 1. 数据来源与方法

| 维度 | 手段 | 本轮产出 |
|---|---|---|
| 结构一致性 | `/claudemd-analyze`（`spec-coherence-audit.js`） | 5/5 检查通过，C=0 H=0 M=0 L=0 |
| 体量与密度 | `wc -c` + Python 统计（§-引用 / HARD 标签 / 括号插入 / 超长行） | §3 表 |
| token 估算 | 字符分类估算（CJK 1 tok/字，其余 3.4 字符/tok） | **估算值，非 tokenizer 实测** |
| hook 开销 | 每个 hook 用合成 payload 跑 3–5 次取均值 | §3.4 表 |
| 运行时命中 | `~/.claude/logs/claudemd.jsonl`（1 076 行，2026-09-05 15:50 → 09-06 23:18，36 会话，3 项目） | §3.5 表；**窗口仅 1.3 天**，日志在 09-05 轮转 |
| 自执行规则遵从 | `scripts/sampling-audit.js --days=30`（本仓库 10 transcripts / 346 turns） | §3.6 |
| 规范工件使用 | `tasks/specs/`、`tasks/lessons.md`、`tasks/rule-candidates-*.md` 内容盘点 | §3.7 |
| harness 对照 | 本会话 Claude Code 系统提示原文（2026-09，Fable 5.1）逐条对照 core | §4 |

**盲区**：无 tokenizer，token 数只有估算；rule-hits 窗口 1.3 天不足以谈趋势；transcript 采样只覆盖本仓库；C1 over-ceremony 分母只有 4；未读其它项目的 spec 使用情况。

---

## 2. 四个问题的直接回答

### 2.1 "版本有点久，有没有适应模型能力提升？"

**日期上不老，结构上老。** changelog 从 v6.9.2（2026-04-21）到 v6.26.0（2026-09-06）共 51 个版本、4.5 个月，平均 2.7 天一版；v6.25.0（08-09）已做过一次压缩，之后 5 个版本又涨回 95.8%。这种节奏下累积的是"防误读补丁"，而当前模型（Claude 5 系）对短声明式规则的遵从已明显好于这些补丁所针对的行为——本仓库 30 天采样：§11 turn-yield 0/41、§10-V 0/346、四段式 0/6 违规（§3.6）。

具体过时假设（§5 详述）：
1. 把 "clarification of clarification" 写进 core（`§0 Hard-AUTH override` 之外又有 `§5 Obvious-follow-on not exempt (clarifies §0 …)`；§11 turn-yield 的 Tell 判据；§2.1 "Skill MUST wording does NOT override"）——这些是为模型把规则读歪时打的补丁。
2. 子代理交付机制用了 4 处、约 2.5 KB 记录一次 2026-09-06 的实测，而 harness 系统提示已用一句话陈述同一事实（"Subagents run in the background; you'll be notified when one completes"）。
3. 路由表只认插件 skill（`sp:dispatching-parallel-agents`），不提 harness 原生 `Agent`（含 fork）/ `Workflow` 原语；`TaskOutput` 已弃用仍在 §11-O 出现。
4. §11 Post-compaction 要求 "Re-Read plan + spec"，而 core 由 harness 每 turn 注入，重读 core 是纯浪费（约 7 k token）；上一轮 SPEC-M5 已指出，未改。

### 2.2 "是否以 spec 为基础、规范化、智能化、自动化？"

| 项 | 判定 | 证据 |
|---|---|---|
| spec 为基础 | **做到** | §2.S 强制 L3、L2 条件性提议；`tasks/specs/` 7 份，6 `implemented` 1 `rejected`，有 frontmatter 与 change log |
| 规范化 | **做到** | 信号只有 `[AUTH REQUIRED]` / `[PARTIAL]` 两个；四段式报告；证据句型有金标准范例；`hard-rules.json` 25 条 HARD 有唯一 id 与执法类型 |
| 自动化 | **半做到** | 15 个 hook / 7 个事件；25 条 HARD 中 6 hook + 2 both 机械执法，**16 self + 1 external 靠自觉**；8 个自执行检测器里 7 个已 "closed"（标注精度 ≤0.17，OPERATOR 已定"需人工复审"），即 64% 的 HARD 规则遵从率**不可机械观测** |
| 智能化 | **部分** | 分级路由（L0–L3）+ 工具升级链（Grep → semantic → impact）是有的；但 §2.1/§4 两张路由表 9 + 22 行，其中 8 行重复；`skillOverrides` 关掉 8 个 gs 主路由后靠 §12 fallback 表降级（操作者 09-01 已裁定不改） |

### 2.3 "有没有分散注意力、占过多上下文？"

**每 turn 固定注入**（本仓库，估算）：

| 来源 | 字节 | 估算 token | 备注 |
|---|---|---|---|
| core `~/.claude/CLAUDE.md` | 23 445（本机 6.25.4）/ 23 944（仓库） | **≈ 6.9 k** | extended Sizing 行自称 "≈ 5.5 k"，与本估算相差 25%，两者都未经 tokenizer 验证 |
| 项目 `CLAUDE.md` | 2 575 | ≈ 0.75 k | 无 `@` 导入；两份 `.claude/plugin_*.md`（21 KB）只被引用不被注入 |
| `MEMORY.md` 索引 | 1 351 | ≈ 0.4 k | 远低于 12 KB 预算 |
| superpowers SessionStart 注入 | 3 108 | ≈ 0.9 k | 与 §2.1 对冲，见 §4 |
| **合计规范侧** | | **≈ 9 k** | 不含 harness 自身系统提示、MCP 说明、skill 列表 |

**结论**：core 占常驻指令的主体，但 token 成本本身不是瓶颈（prompt cache 下边际成本低，OPERATOR §13.1 与 2026-07-10 优化方案 B3 均已如此判断）。真正的注意力代价在两处：

1. **密度**：core 每非空行均 144 B，最长行 1 261 B（§11 turn-yield，core:239）；86 处 ≥15 字符的括号插入合计 3 818 B（16%）；123 个 `§` 交叉引用、32 个 `→ §EXT` 指针。读者要在一行里同时处理规则、例外、例外的例外和指针。
2. **重复 / 冲突指令**：与 harness 系统提示重叠约 1.5–2 KB（§4），其中两条**直接冲突**（报告范例用括号 + 破折号，harness 禁止；§0 默认 ASK vs harness "proceed without asking"）。冲突段每次写报告都要在两套风格指南间裁决，这是比字节数更实在的注意力损耗。

**hook 侧不构成注意力负担**：4 个 PreToolUse hook 单次 7–8 ms，正常路径注入 0 字节；Stop 五个 hook 合计约 86 ms（§3.4）。

### 2.4 "有没有冗余需要优化？"

有，且 spec 违反了自己的 "one home per fact"。可净删除的冗余清单见 §6，合计估算 core −3.0 KB、extended −2.3 KB；另有约 0.8 KB 应从 core **移入**（`aggressive` 跳过清单）而非删除。

---

## 3. 量化数据

### 3.1 体量与预算

| 文件 | 字节 | 上限 | 占比 | 距上限 |
|---|---|---|---|---|
| core | 23 944 | 25 000 | 95.8% | 1 056 |
| extended | 45 899 | 50 000 | 91.8% | 4 101 |
| OPERATOR（不注入） | 15 514 | — | — | — |
| changelog（不注入） | 138 145 | — | — | — |

### 3.2 core 各节字节（前 6）

| 节 | 字节 | 占 core |
|---|---|---|
| §11 SESSION | 3 667 | 15.3% |
| §10 REPORT | 1 918 | 8.0% |
| §2.1 ROUTE | 1 873 | 7.8% |
| §2 LEVEL | 1 834 | 7.7% |
| §8 Verify-before-claim | 1 517 | 6.3% |
| §1 IDENTITY | 1 479 | 6.2% |

§11 一节比 §5 AUTH + §7 VALIDATE 两节之和还大；其中 turn-yield 单条 1 261 B。

### 3.3 密度指标

| 指标 | core | extended |
|---|---|---|
| `§` 引用 | 123 | 121 |
| `→ §EXT` 指针 | 32 | 2 |
| HARD 标签 | 13 | 27 |
| MUST / NEVER / banned | 21 | 10 |
| 加粗词条 | 102 | 163 |
| ≥15 字符括号插入（个 / 字节） | 86 / 3 818 | 158 / 7 547 |
| 破折号 | 34 | 97 |
| >700 B 的行 | 1 | 4 |
| 非空行均长 | 144 B | 116 B |

### 3.4 hook 开销（本机，合成 payload，均值）

| hook（事件） | 均值 ms | 正常路径注入 |
|---|---|---|
| pre-bash-safety-check（PreToolUse:Bash） | 7 | 0 B |
| banned-vocab-check（PreToolUse:Bash） | 8 | 0 B |
| ship-baseline-check（PreToolUse:Bash） | 8 | 0 B |
| memory-read-check（PreToolUse:Bash） | 8 | 0 B |
| session-extended-read（PreToolUse:Read） | 11 | — |
| transcript-vocab-scan（PostToolUse） | 1 | — |
| residue / sandbox / mem-audit / structure / summary（Stop） | 13 / 18 / 19 / 2 / 34 | — |

每次 Bash 调用约 31 ms 串行开销；manifest 里声明的超时 3–5 s 有百倍余量。

### 3.5 运行时命中（1.3 天，36 会话，3 项目）

| 规则 | deny | 放行 / 其它 | 备注 |
|---|---|---|---|
| §8-rm-rf-var | **93** | provenance 94 / validated 77 / bypass 3 | 28/36 会话 ≥1 次 deny；`extra.matched` 全部为 `rm -rf $S` / `$SB` / `$f` 类未校验变量，规则按设计生效，摩擦来自代理写沙箱清理的习惯，不是误拦（2026-09-07 更正：初稿误归因为 HK-H1） |
| §8-npx | 6 | allow-local 616 | 每次 npx 都记一行，deny 率 1% |
| §8-curl-sh | 6 | — | 5 次 fetch-execute / reverse-shell 形状、1 次管道形状，均来自其它会话（2026-09-07 更正：初稿称本会话首条命令被拦，有误） |
| §10-V | 6 | bypass 1 | |
| §11-memory-read | 3 | bypass 3 / fail-open 10 | fail-open 全为 `mem-index-missing` |
| §7-ship-baseline | 2 | pass 53 / known-red 3 | |
| §13.1-extended-read | — | read 12 | **extended 在 33% 会话被加载**，不是"偶尔" |
| §8.V4 sandbox | — | warn 42 | |

### 3.6 自执行规则采样（本仓库 30 天，10 transcripts / 346 turns）

| 规则 | 违规 / 机会 | 检测器状态 |
|---|---|---|
| §10-V | 0 / 346 | collecting |
| §11-turn-yield | 0 / 41 | collecting |
| §iron-law-2 | 1 / 6 | closed（精度未达 0.8） |
| §10-four-section-order | 0 / 6 | closed |
| §11-post-compaction | 1 / 1 | closed |
| §5-hard-auth | 3 / 3 | closed（预登记为 FP 偏高） |
| §10-honesty / §7-bugfix-anchor | 0 / 0 | closed |
| C1 over-ceremony | 0 / 4 L0-L1 段 | 阈值 <5% → 维持 superpowers |

解读：两个仍在收集的检测器给出 0 违规，说明这两条 HARD 在当前模型上"被遵守"，其冗长的澄清文本失去了针对对象；closed 的六个不能用于任何遵从率论断。

### 3.7 规范自有工件的实际使用

| 工件 | 规范条款 | 现状 | 判定 |
|---|---|---|---|
| `tasks/specs/*.md` | §2.S | 7 份，6 implemented / 1 rejected | **活** |
| `tasks/rule-candidates-2026-04.md` | §13.2 | 3 次 batch review（05-10 / 07-10 / 08-24），最新日期 2026-09-23（前瞻排期） | **活** |
| `tasks/lessons.md` | §10-R Lessons file（"Read at session start"） | 6 条，最后一条 **2026-05-09**，4 个月无新增 | **死**：每会话一次读取义务换 0 收益 |
| `tasks/<slug>-paused.md` | §11 | 0 份 | 正常（无中断任务） |
| 5 个被引用的记忆文件 | core:223 / ext:266,454,505,541 | **0/5 存在**于本机记忆目录 | 死引用（Round 14 SPEC-M12），仍未修 |

---

## 4. 与当前 harness 系统提示的重叠与冲突

本会话 Claude Code 系统提示（2026-09，Fable 5.1）已原生包含下列约束。规范按 §3 TRUST 高于 harness，所以重叠不改变行为，但**每一条都是注意力重复；冲突条则每次都要裁决**。

| # | harness 原生条款 | 规范对应 | 关系 | 建议 |
|---|---|---|---|---|
| H1 | "Before ending your turn … do that work now … Do not stop because the context is long" | core:239 §11 Mid-SPINE turn-yield（1 261 B） | **重叠**；规范增量只有 Tell 判据 + 子代理等待触发 | 压到 ≤500 B，实测理由移 changelog |
| H2 | "Subagents run in the background; you'll be notified when one completes … Never fabricate a pending agent's results" | core:239 子代理触发 + ext:312 §11-O + ext:350 §12 第二例外 + ext:446-453 Recent changes | **重叠 ×4 处** | 保留 core 一句 + changelog，其余删 |
| H3 | "No em-dashes, no parentheticals" 于用户可见文本 | core:158 `Done: … (Checked: …)` 范例；ext B.2 全部范例；core 34 / ext 97 个破折号 | **冲突** | 范例改为句式："Checked: pre-fix TypeError; post-fix 7 passed."（字节中性） |
| H4 | "proceed without asking … Stop only for destructive actions or genuine scope changes" | core:13 Initial-prompt ambiguity 默认 (a) ASK once（可逆性 >10 min） | **冲突（弱）** | 候选：默认改为 (b) 内联声明，AUTH 相关才 ASK；属 minor 放宽，走 §13 META |
| H5 | 记忆文件格式、4 类型、"add a one-line pointer in MEMORY.md"、"Don't save what the repo already records" | core:232 Auto-memory "Always skip"；ext:522 "After any memory write: refresh MEMORY.md" | **重叠** | 删 core 的 skip 列表与 ext 的 index-refresh 行（保留触发 1–3、tag 语法、read-the-file） |
| H6 | "Report outcomes faithfully: if tests fail, say so with the output; if a step was skipped, say that" | §10 honesty / Iron Law #2 | 重叠（规范更可操作） | 保留规范 |
| H7 | "Reference code as file_path:line_number" | core:162 | 重叠 | 忽略 |
| H8 | "For actions hard to reverse … confirm first unless durably authorized" | §5 AUTH | 重叠（规范给出类别表 + 信号） | 保留规范 |
| H9 | superpowers 6.3.0 SKILL.md 自述 "User instructions (CLAUDE.md …) take precedence over skills" | core:100 "Skill MUST wording does NOT override §2.1" | **重叠**：对手方已让步；C1 = 0/4 | 候选删（约 300 B），分母小，先降为一句 |
| H10 | "When the conversation grows long, some or all of the current context is summarized" | core:229 Post-compaction "Re-Read plan + spec" | 冲突：core 每 turn 自动注入 | 改为 "Re-Read plan；extended 仅在本任务曾加载时重读" |
| H11 | `Agent`（fork / general-purpose）、`Workflow`、`ScheduleWakeup` 原生原语 | §2.1 "2+ disjoint tasks → sp:dispatching-parallel-agents"；ext:312 "TaskOutput is deprecated" | 缺失 / 过时 | §2.1 改为 "→ Agent tool（fork 继承上下文）；sp:dispatching-parallel-agents 可选" |

---

## 5. 模型能力提升后已过时的设计假设

1. **"模型会把规则读歪，所以要写澄清"**：core 有 3 处 "clarifies …" 式条款、1 处 "Two bullets below carry a stronger tag" 元说明、1 处 Tell 判据；采样 0/41 与 0/346 说明当前模型不需要这些拐杖。删除它们不改变任何规则语义。
2. **"模型看不懂指针，所以指针要带解释"**：core:60 "a core citation without a §EXT pointer is a defect" 是维护者的规则，写在 Agent 每 turn 读的 core 里。移 OPERATOR。
3. **"事故要写进规则才记得住"**：§11 turn-yield 内嵌 "measured 2026-09-06"、§11-O 内嵌 "3h02m / 126 tool calls / 230 ms"。当前 harness 已把该事实写进系统提示；规则只需说 "yield 即交付"，理由归 changelog（§13 META 本来就是这么分工的）。
4. **"子代理是插件功能"**：§2.1 / §4 / §11-O / §12 只以 sp/gs skill 命名并行与评审；harness 原生 `Agent` 带 fork、后台运行与完成回调，`Workflow` 提供确定性编排。规范应把原语写为主路径，skill 写为封装。
5. **"上下文是稀缺资源，所以要预算字节"**：25 K / 50 K 硬上限本身仍有价值，但理由应从 "占上下文" 改为 "稀释指令跟随"（OPERATOR B3 已这样判断）；Sizing 行的 "≈ 5.5 k tokens" 与本轮估算 6.9 k 不一致，应用 API `count_tokens` 实测一次后钉死，或删掉这个未验证数字。
6. **"lessons 文件是跨会话记忆"**：`tasks/lessons.md` 自 05-09 无新增，而 MEMORY.md + claude-mem-lite 两层记忆已接管该职能。§10-R 的 "Read at session start" 每会话付一次读取，4 个月零回报。

---

## 6. 冗余清单（可净删除）

字节为估算。位置按 v6.26.0 行号。

| # | 位置 | 内容 | 处置 | core Δ | ext Δ |
|---|---|---|---|---|---|
| R1 | core:239 | turn-yield 的实测理由、两个替代方案为何失败、Tell 判据 | 保留四个触发 + "yield 即交付" 一句；理由 → changelog | −750 | |
| R2 | ext:312、ext:350 第二例外、ext:446-453 | 同一 2026-09-06 事实的三次复述 | ext:312 保留一句 "输出在 turn 结束进入上下文；不能 yield 时轮询指定文件"；§12 第二例外保留一句；Recent changes 改为 3 行摘要 | | −1 400 |
| R3 | core:232 "Always skip" 行；ext:520、ext:522 | 与 harness 记忆规则逐字重复 | 删 | −200 | −150 |
| R4 | core:25 §0.1 | 分层升降级是操作者动作 | 保留 "core ≤25 K / ext ≤50 K bytes；超限 → 下版净删除" 一句，其余 → OPERATOR | −220 | |
| R5 | core:60 | "core citation without §EXT pointer is a defect" 维护者规则 | → OPERATOR | −200 | |
| R6 | core:100 | "Skill MUST wording does NOT override" 整段 | 压为一句（superpowers 自身已让步；C1=0） | −300 | |
| R7 | core:129 | "Obvious-follow-on not exempt (clarifies §0 …)" | 合并进 §0 Hard-AUTH override 一句 | −250 | |
| R8 | core:226 "Two bullets below carry a stronger tag" 元说明 | Round 14 SPEC-L 已指出实为三条 | 删元说明，直接在条目上标 HARD | −180 | |
| R9 | ext:395 §13 "Today: 6 hook / 16 self / 2 both / 1 external" | 与 hard-rules.json 双写，已漂过一次（v6.13 时 14 self） | 改为指针 | | −120 |
| R10 | ext:295-298 §10-R Lessons file | 机制已死（§3.7） | 降为 SHOULD 或删 "Read at session start" | | −200 |
| R11 | core:223、ext:266/454/505/541 | 5 个不存在的记忆文件引用 | 删引用或改为 "若存在" | −120 | −250 |
| R12 | core:158/162、ext:430-441 | 范例中的括号与破折号 | 改句式，不删（H3） | 0 | 0 |
| **合计** | | | | **≈ −2 220** | **≈ −2 120** |
| 反向 | ext:472 `aggressive` 跳过清单 | L0–L2 禁止加载 ext，但清单只在 ext（Round 14 SPEC-M7；本仓库正设 aggressive） | 一句移入 core §5.1 | +180 | −0 |
| 反向 | §2.1 | 原生 `Agent` / `Workflow` 原语一句 | 加 | +120 | |
| **净** | | | | **≈ −1 900** | **≈ −2 100** |

净删后 core 约 22 000 B（88%），extended 约 43 800 B（87.6%），恢复一个 minor 版本的成长空间。

另有两处 **结构性重复** 不建议本轮动：§2.1（9 行）与 §4 Routing（22 行）重叠 8 行——core 表是 L0–L2 的子集，设计上如此；§7 Iron Law #1 一行版 + ext 详版——L2 不加载 ext 所以必须双写。

---

## 7. 建议

### 7.1 下一版：v6.27.0 "harness-convergence"（minor，L3，一次发版）

顺序按 "先纯减、后改语义"：

1. **R11 死引用**（纯减，无语义）。
2. **R1 / R2 / R9**：事实归 changelog，规则留一句（纯减）。
3. **R3 / R4 / R5 / R8**：维护者文本与 harness 重复文本移出 core（纯减）。
4. **R12 + H10**：范例句式化；Post-compaction 去掉 "重读 spec"（语义修正，Round 14 SPEC-M5）。
5. **aggressive 清单入 core**（Round 14 SPEC-M7）。
6. **H11**：§2.1 加原生原语一句；ext:312 删 `TaskOutput` 提法。
7. **R6 / R7**：澄清条款压缩——先降为一句，观察 30 天采样后再决定是否全删。
8. **R10**：Lessons file 降级。

约束：零新增 HARD；每处改动整行 golden-pin（`feedback_spec_text_gate_golden_pin.md`）；发版后跑 `/claudemd-analyze --strict` + `spec-structure.test.js`；Sizing 行按 `wc -c` 重写。

### 7.2 需要数据后再做

- **H4**（默认 ASK → 内联声明）：先在 sampling-audit 加 "ASK 次数 / 任务" 计数，30 天后看用户回答率；回答多为 "就按你说的" → 改默认。
- **R6 全删**：C1 分母只有 4，跑满 30 天 ≥30 段再定。
- **Sizing token 数**：用 Anthropic `count_tokens` 实测 core / extended 一次，写死数字与日期；此前不再在 spec 里出现 "≈ 5.5 k"。

### 7.3 hook 侧

- **HK-H1**（Round 14）pattern 3d 误拦：把一行式当**数据**（echo 写文件 / 提交信息 / grep / heredoc 体）以及裸 `import socket, subprocess, os` 都被拦，本轮沙箱复现 5/5；频次低（§8-curl-sh 1.3 天共 6 次 deny，不全是 3d），但每次都挡住维护者写这类测试与文档的正当命令。修法：候选提取加命令位锚点 + 先剥 heredoc 体 + 执行原语要求调用形。
- 93 次 rm-rf deny 不是误拦（见 §3.5 更正），不需要改 hook；若要降摩擦，方向是让代理在写沙箱清理时直接用 `: "${V:?}" && rm -rf "$V"` 或同命令内 `mktemp`，deny 文案已经在教这一点。

### 7.4 明确不建议做的事

- **不做 core 段落 demote**：2026-06-03 已裁定为 category error，"0 telemetry = 基础性规则被读并遵守"。
- **不卸载 superpowers**：C1 = 0/4，低于预登记 5% 阈值。
- **不因 skillOverrides 改 §4**：09-01 已裁定，`doctor` 的 `routing:skills-enabled` 是常设检测器。
- **不把自执行检测器的 closed 率写进任何结论**：A4 已关闭，精度 ≤0.17。
- **不新增 "harness 冲突时如何裁决" 的规则**：§3 TRUST 已给出序；H3/H10 是范例与措辞问题，改范例即可。

---

## 8. Round 14 规范项状态

| 项 | 状态 | 本轮补充 |
|---|---|---|
| SPEC-H1 信任序压过用户 | Open | 与 H4 同源；建议合并处理 |
| SPEC-H3 令牌归属 | Open | 本轮 bypass 令牌 rm-rf 3 / memory-read 3 / §10-V 1 |
| SPEC-H4 §5 无 manifest 行 | Open | — |
| SPEC-M5 Post-compaction 重读 spec | Open | 本轮列为 H10，纳入 7.1 |
| SPEC-M7 aggressive 清单不在 core | Open | 纳入 7.1 |
| SPEC-M8/M9 子代理等待重复 | Open | 本轮量化为 4 处约 2.5 KB（R1/R2） |
| SPEC-M12 死记忆引用 | Open | 本轮复核仍 0/5（R11） |

---

## 9. 未验证项与风险

- token 数全部为估算；两处数字（本估算 6.9 k、spec 自称 5.5 k）都不能作为发版依据。
- rule-hits 只有 1.3 天，§3.5 的比例不可外推到月度。
- 本轮判断 "当前模型不需要澄清条款" 依据的是 0/41 与 0/346 两个采样，均来自本仓库、当前模型；换模型或外部项目未测。删 R6/R7 前应保留 30 天回滚窗口（§13 META 30-day grace 本来就要求）。
- 本机生效的是 v6.25.4，不是被审的 v6.26.0；本轮所有行为观察（含本会话）都在旧版下发生，差异只在 §11 turn-yield 一条。
- 未验证 harness 系统提示在其它入口（IDE / web）是否与本会话一致；§4 的重叠表以 CLI 2026-09 版本为准。

---

## 10. 执行记录（2026-09-07）

按 §7.1 / §7.3 执行，分支 `spec-v6.27.0-harness-convergence` 三次提交后 ff 到 `main` 并 push（`10e313a`）；pre-tag 评审（新鲜子代理，60 个探针）后再修一轮（下表末行），随后打 tag。

| 项 | 结果 | 证据 |
|---|---|---|
| v6.27.0 spec（R1–R8、R10–R12、H10、H11、aggressive 入 core） | 落地 | core 23 944 → 23 405 B（−539）；extended 45 899 → 44 364 B（−1 535）；OPERATOR 15 514 → 15 996 B（+482）；§11 turn-yield 1 261 → 921 B |
| R9（§13 分区计数） | **未做** | `hard-rules-drift.test.js:323` 要求该行存在并与 manifest 联检，不是双写漂移，初稿判断有误 |
| 三条金钉 | 同 commit 重钉 | `spec-structure.test.js` 18/18 |
| 门 | 全绿 | `version-cascade-check` ok（v6.27 ×3、Sizing ±20B、0.79.0 ×6）；`spec-coherence-audit --strict` 5/5；`npm run check` exit 0，node 1074/1074，hook 套件 763/763 等 |
| HK-H1 pattern 3d | 修复 | 语料 +15 行（R5），修前 727/732（5 个 pass 行 deny），修后 732/732；沙箱 12 形状 5 allow / 4 deny / 3 allow 全部符合预期；延迟 deny 路径 94 → 101 ms、普通命令 61 → 63 ms（各 10 次） |
| 7.2 三项（默认 ASK、R6 全删、tokenizer 实测） | 未做 | 按计划等数据 |
| 版本位点 | 0.78.0 → 0.79.0 六处；README / plugin.json / marketplace.json v6.26 → v6.27 | `version-cascade-check` |
| pre-tag 评审第 1 轮修复 | H1 锚点过窄（22 个执行位置从 deny 变 allow）、H2 heredoc 剥离无引号状态可绕过、H3 `REVSH_EXEC` 漏 `from subprocess import call` 族；M1 §4/§11-O 与 §2.1 并行原语不一致、M2 changelog 对 harness 覆盖范围的断言过头、M3 §12 例外窄于 §11 触发 | 语料 +21 行 R6：修前 734/753（19 个 deny 行 allow），修后 753/753；spec 三处改动同 commit 重钉；延迟数字撤回（同一 hook 两次测量 63 vs 146–162 ms，噪声大于改动） |
| pre-tag 评审第 2 轮修复 | DH1 `su <user> -c` / `su -s … -c` 未展开、DH2 heredoc 引号状态不跨行、DH3 `<<<` here-string 被当成 opener、DH4 新加的 `fork(` 误伤普通分叉服务器、DH5 `tasks/lessons.md` 漏提交；DL1 Sizing 差 1 字节、DL3 §12 fallback 行、DL4 §12 例外按 spawn 位置而非按等待对象 | 语料 +6 行 R7：修前 753/759（4 个 deny 行放行、2 个普通命令被拦），修后 759/759，连续三次 |
| pre-tag 验证轮修复 | V-H1 `su - <user> -c` 登录简写未展开（0.78.0 拦、本版放行）；V-L1 Sizing 差 1 字节、V-L3 本表门禁行陈旧 | 语料 +4 行 R8：修前 761/763，修后 763/763，连续三次；六种 su 形式沙箱复核全部 deny |

净删幅度低于 §6 的估算（core 估 −1 900，实得 −539）：R1 保留了 Tell 与四触发的完整措辞（921 B 而非估算的 500 B），R6/R7 只压缩未删，R9 取消，aggressive 清单与 `Agent` 原语各加回约 200 B。extended 实得 −1 535 与估算 −2 100 同向。
